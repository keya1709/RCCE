services:
  - type: web
    name: rcce-estimator
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
      - key: OPENWEATHER_API_KEY
        sync: false
